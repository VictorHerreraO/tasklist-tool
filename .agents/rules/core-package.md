---
trigger: glob
globs: packages/core/**
---

## Package - Core

- Before making any changes to the core package assess the impact on the `package/extension` and `package/mcp` modules
- Make sure that your changes do not cause any regression issues
- If you are unable to avoid regression issues **escalate to the user**