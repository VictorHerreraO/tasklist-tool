# Completion Guidelines
Before completing a task, ensure:
1. The code compiles without errors (`npm run compile`).
2. Lint check passes (`npm run lint`).
3. All relevant tests pass (`npm run test`).
4. Any new features or changes are properly documented (e.g., updating README or templates).
